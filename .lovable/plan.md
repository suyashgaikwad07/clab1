

## Plan: Add Download .c File Button

Add a "Download" button next to the existing "Copy Code" button in the top bar. Clicking it will generate and download a `.c` file with the program's code.

### Changes

**`src/pages/Index.tsx`**:
1. Import `Download` icon from `lucide-react`
2. Add a `handleDownload` function that creates a Blob from `selected.code`, generates a temporary download link, and triggers the download with a filename derived from `selected.title` (e.g., `reverse-array.c`)
3. Add a Download button next to the Copy button in the top bar, styled consistently with the existing button but using a secondary/outline style to distinguish it

The filename will be generated by converting the program title to kebab-case (lowercase, spaces to hyphens) with a `.c` extension.

